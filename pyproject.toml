[project]
name = "table-viewer"
version = "0.2.0"
description = "A fast desktop viewer for CSV and SQLite files"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.3.3",
]
